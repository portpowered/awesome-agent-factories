## Codebase Patterns
- Root Go module is `github.com/portpowered/awesome-agent-factories` with `go 1.24`; run `go build ./internal/checks` and `go test ./...` from repository root.
- `internal/checks` is a `package main` CLI with responsibilities split across `main.go`, `readme.go`, `links.go`, `sections.go`, `alphabetical.go`, and `duplicates.go`.
- No package.json or CI tooling in this worktree; Go quality gates are `go build` and `go test ./...`.
- Use the standard Awesome badge markup: `[![Awesome](https://awesome.re/badge.svg)](https://awesome.re)` on the H1 line.
- `docs/internal/customer-ask.md` contains suggested opening copy and Contents anchor patterns for later stories.

## 2026-06-06T00:00:00Z - awesome-readme-foundation-001
- Established README identity: Awesome H1 with badge, inline SVG logo mark (three connected nodes), blockquote tagline, and factual agent-factory definition; removed placeholder `# awesome-agent-factories` heading.
- Files changed: README.md
- **Learnings for future iterations:**
  - Story 001 is intentionally limited to opening narrative only; Scope, Contents, category skeletons, and footer notes are separate stories (002–005).
  - Place the H1 first to satisfy "opens with" acceptance criteria; logo treatment can follow immediately after.
  - Customer-ask suggested copy aligns well with PRD acceptance criteria for the opening block.
---

## 2026-06-06T12:00:00Z - awesome-readme-foundation-002
- Added `## Scope` section with introductory statement, Included list (8 items covering orchestration, teams/swarms/crews/societies/supervisors, delegation/routing/handoffs, protocols, shared state, group benchmarks, coordination research, production case studies), and Excluded unless directly relevant list (7 items); language is neutral and factual per customer-ask template.
- Files changed: README.md
- **Learnings for future iterations:**
  - `docs/internal/customer-ask.md` lines 99–126 provide copy-ready Scope section text that matches PRD acceptance criteria exactly.
  - Scope section belongs immediately after the opening narrative and before Contents (story 003).
---

## 2026-06-07T00:00:00Z - phase-4-go-readme-checker-core-001
- Bootstrapped root `go.mod` with module path `github.com/portpowered/awesome-agent-factories` and Go 1.24 toolchain.
- Added `internal/checks` package skeleton with `main.go`, `readme.go`, `links.go`, `sections.go`, `alphabetical.go`, and `duplicates.go` preserving planned responsibilities via shared types (`ReadmeDocument`, `Section`, `ResourceEntry`, `CheckResult`).
- Verified `go build ./internal/checks` and `go test ./...` succeed from repository root.
- Files changed: go.mod, internal/checks/main.go, internal/checks/readme.go, internal/checks/links.go, internal/checks/sections.go, internal/checks/alphabetical.go, internal/checks/duplicates.go
- **Learnings for future iterations:**
  - `internal/checks` uses `package main` so `go run ./internal/checks` works without a separate `cmd/` directory.
  - Story 001 intentionally leaves `run()` as a no-op; story 002+ wire README loading and validation rules.
  - No external Go dependencies yet, so `go.sum` is not required.
---
