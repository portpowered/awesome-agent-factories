## Codebase Patterns
- Phase 5 core CI: `.github/workflows/ci.yml` uses `actions/checkout@v4`, `actions/setup-go@v5` with `go-version: stable`, and `go mod download`; full gate steps (gofmt, tests, README checks) are added incrementally per PRD stories 002–004.
- No package.json in this worktree; Go quality gates are `go build ./...`, `make check`, `make test`, and `git diff --check`.
- Use the standard Awesome badge markup: `[![Awesome](https://awesome.re/badge.svg)](https://awesome.re)` on the H1 line.
- `docs/internal/customer-ask.md` contains suggested opening copy and Contents anchor patterns for later stories.
- Phase 4 lint config: `.golangci.yml` uses golangci-lint v2 schema (`version: "2"`); `gofmt`/`gofumpt` live under `formatters.enable`, other linters under `linters.enable`.
- This worktree fast-forwarded `phase-4-go-readme-checker-core` to obtain `go.mod` and `internal/checks/` before adding Makefile/lint automation.
- Optional-tool Makefile targets: guard with `command -v <tool>`; print tool name, purpose, `go install` hint, and upstream docs URL to stderr before exiting 1.
- README `## Contents` lists Scope plus nine in-TOC curated sections (Theories through Examples and Templates); `Related Lists` and `Contributing` are in `contentsExcludedSections` (section headings required, omitted from Contents per awesome-list convention); `Community` stays as a footer section, not Contents navigation.
- Wrap README `## Contents` link list with `<!--lint disable awesome-toc-->` / `<!--lint enable awesome-toc-->` so `npx awesome-lint` accepts `## Community` (and other excluded sections) omitted from Contents navigation.
- README checker warns when Blog Posts (and other resource) descriptions lack agent-factory scope keywords (coordination, orchestration, delegation, routing, handoffs, shared state, or group-level evaluation); weave at least one keyword into each description to avoid warnings.

## 2026-06-06T00:00:00Z - awesome-readme-foundation-001
- Established README identity: Awesome H1 with badge, inline SVG logo mark (three connected nodes), blockquote tagline, and factual agent-factory definition; removed placeholder `# awesome-agent-factories` heading.
- Files changed: README.md
- **Learnings for future iterations:**
  - Story 001 is intentionally limited to opening narrative only; Scope, Contents, category skeletons, and footer notes are separate stories (002–005).
  - Place the H1 first to satisfy "opens with" acceptance criteria; logo treatment can follow immediately after.
  - Customer-ask suggested copy aligns well with PRD acceptance criteria for the opening block.
---

## 2026-06-06T12:00:00Z - awesome-readme-foundation-002
- Added `## Scope` section with introductory statement, Included list (8 items covering orchestration, teams/swarms/crews/societies/supervisors, delegation/routing/handoffs, protocols, shared state, group benchmarks, coordination research, production case studies), and Excluded unless directly relevant list (7 items); language is neutral and factual per customer-ask template.
- Files changed: README.md
- **Learnings for future iterations:**
  - `docs/internal/customer-ask.md` lines 99–126 provide copy-ready Scope section text that matches PRD acceptance criteria exactly.
  - Scope section belongs immediately after the opening narrative and before Contents (story 003).
---

## 2026-06-07T00:50:00Z - phase-4-makefile-and-lint-config-001
- Added root `.golangci.yml` (v2 schema with `run.timeout: 5m`, govet/staticcheck/errcheck/ineffassign/unused/revive/misspell linters, gofmt/gofumpt formatters), `.markdownlint.yaml` (MD013/MD033/MD041 disabled, MD024 siblings_only), and `.lychee.toml` (max_retries 2, timeout 20, accept codes, localhost/127.0.0.1 excludes) per `docs/internal/customer-ask.md` Phase 4.
- Fast-forwarded `phase-4-go-readme-checker-core` into this worktree so `golangci-lint run --config .golangci.yml ./internal/...` can validate config against checker code.
- Files changed: `.golangci.yml`, `.markdownlint.yaml`, `.lychee.toml`
- **Learnings for future iterations:**
  - golangci-lint v2 (2.x) rejects v1 config without `version: "2"`; migrate formatters (`gofmt`, `gofumpt`) to the `formatters` section.
  - Config parse success is distinct from lint findings; story 001 only requires no parse errors.
  - `go build ./internal/checks` emits a `checks` binary at repo root—remove before committing.
---

## 2026-06-07T01:15:00Z - phase-4-makefile-and-lint-config-002
- Added root `Makefile` with `.PHONY` targets `all`, `lint`, `check`, `links`, `fmt`, and `test` matching `docs/internal/customer-ask.md` Phase 4; `all` delegates to `check`, `check` runs `go run ./internal/checks`, `test` runs `go test ./...`, `fmt` runs `gofmt -w internal`.
- Verified `make all`, `make check`, `make test`, and `make fmt` exit 0 against the current README and checker tests.
- Files changed: `Makefile`
- **Learnings for future iterations:**
  - Story 002 only requires core targets (`all`, `check`, `test`, `fmt`); `lint` and `links` are declared with direct tool invocations for stories 003–004 to add missing-tool guidance.
  - Remove the `checks` binary after `go build ./internal/checks` before committing.
  - `make fmt` is idempotent when checker Go files are already formatted.
---

## 2026-06-06T17:49:31Z - phase-4-makefile-and-lint-config-003
- Wired `make lint` with `command -v golangci-lint` guard, stderr install guidance (`go install` path and upstream docs URL), and explicit `golangci-lint run --config .golangci.yml ./...`.
- Fixed pre-existing golangci findings so `make lint` exits 0: grouped regexp vars in `links.go` (gofumpt) and added package comment in `main.go` (revive).
- Verified missing-tool path with `PATH="/usr/bin:/bin" make lint` (exit 1, human-readable stderr).
- Files changed: `Makefile`, `internal/checks/links.go`, `internal/checks/main.go`
- **Learnings for future iterations:**
  - `golangci-lint run --fix` auto-applies gofumpt grouping without a separate `gofumpt` binary.
  - Story 003 scope includes making checker code pass the wired linter, not only the Makefile guard.
  - Story 004 can mirror the same `command -v` + stderr install-hint pattern for `lychee`.
---

## 2026-06-07T02:30:00Z - phase-4-makefile-and-lint-config-004
- Wired `make links` with `command -v lychee` guard, stderr install guidance (`brew install lychee` and upstream docs URL), and `lychee README.md docs/*.md` invocation that picks up root `.lychee.toml` automatically.
- Verified missing-tool path with `PATH="/usr/bin:/bin" make links` (exit 1, human-readable stderr).
- Verified installed path with `make links` (exit 0, 138 links OK, no config parse errors).
- Files changed: `Makefile`
- **Learnings for future iterations:**
  - Lychee auto-discovers `.lychee.toml` in the repo root; no explicit `--config` flag needed when run from repository root.
  - `docs/*.md` glob in the Makefile expands to the four existing docs markdown files; lychee honors exclude rules for localhost/127.0.0.1 from `.lychee.toml`.
  - Mirror the lint target's `command -v` + stderr install-hint pattern for any future optional-tool Makefile targets.
---

## 2026-06-07T03:00:00Z - phase-4-makefile-and-lint-config-005
- Verified existing `.gitignore` factory/monorepo entries (`.claude`, `tasks/`, `docs/internal`, `factory/Makefile`, `factory/docs`) and editor/OS/build patterns remain unchanged in meaning; confirmed `.editorconfig` still matches Phase 2 charset, LF, final newline, markdown trailing-whitespace exception, Go tabs, and two-space YAML/TOML/JSON/Markdown indent.
- Added `/checks` to `.gitignore` so the root binary emitted by `go build ./internal/checks` is ignored without broad Go artifact patterns.
- Files changed: `.gitignore`
- **Learnings for future iterations:**
  - `go build ./internal/checks` names the output binary `checks` at the repository root; anchor with `/checks` to avoid ignoring unrelated paths.
  - Hygiene story 005 is verification-first: only extend ignore rules when the checker introduces artifacts not already covered.
  - No `docs/internal/processes/` directory exists yet in this worktree; skip relevant-files updates until that layout lands.
---

## 2026-06-07T04:00:00Z - phase-4-makefile-and-lint-config-006
- Added thin `scripts/check-readme.sh` and `scripts/normalize-readme.sh` wrappers that `cd` to the repository root and `exec make check` / `exec make fmt` without duplicating checker or formatting logic.
- Verified both scripts are executable, runnable from the repository root, and exit with the same results as their Makefile targets.
- Files changed: `scripts/check-readme.sh`, `scripts/normalize-readme.sh`
- **Learnings for future iterations:**
  - Use `cd "$(dirname "$0")/.."` plus `exec make <target>` so scripts work from any working directory and propagate exit codes.
  - Story 006 adds scripts rather than Makefile help text because the customer-ask directory layout includes `scripts/` entry points.
  - Story 007 will document `make check`, `make test`, and `make all` in CONTRIBUTING.md; scripts remain optional aliases.
---

## 2026-06-07T05:00:00Z - phase-4-makefile-and-lint-config-007
- Added a **Local checks** section to `CONTRIBUTING.md` naming `make check`, `make test`, and `make all` as primary pre-submit commands; documented optional `make lint` (golangci-lint) and `make links` (lychee) with upstream install pointers; noted script wrappers and that GitHub Actions/scheduled CI are not configured yet.
- Files changed: `CONTRIBUTING.md`
- **Learnings for future iterations:**
  - Place **Local checks** immediately before **Before you open a pull request** so contributors run Makefile targets before the submission checklist.
  - Mirror Makefile stderr install hints in CONTRIBUTING.md for optional tools to keep docs aligned with missing-tool behavior.
  - All Phase 4 PRD stories are complete after 007; create the PR when no existing PR is open.
---

## 2026-06-07T12:00:00Z - phase-5-core-ci-workflow-001
- Added `.github/workflows/ci.yml` with workflow name `CI`, `pull_request`/`push` triggers on `main`, single `go-checks` job on `ubuntu-latest`, `actions/checkout@v4`, `actions/setup-go@v5` (`go-version: stable`), and `go mod download`.
- Files changed: `.github/workflows/ci.yml`
- **Learnings for future iterations:**
  - Story 001 is setup-only; format check, `go test ./...`, and README validation land in stories 002–004 per `docs/internal/customer-ask.md` Phase 5 template.
  - `docs/internal/customer-ask.md` lines 389–422 provide the full target `ci.yml` shape for later stories.
  - Local typecheck for Go changes is `go build ./...`; no separate `make typecheck` target exists in this repo.
---

## 2026-06-07T12:30:00Z - phase-5-core-ci-workflow-002
- Added read-only **Format check** step to `.github/workflows/ci.yml` using `test -z "$(gofmt -l internal)"` after dependency download; matches local `make fmt` scope without `gofmt -w`.
- Files changed: `.github/workflows/ci.yml`
- **Learnings for future iterations:**
  - Use `gofmt -l internal` (list-only) in CI; local `make fmt` uses `gofmt -w` for fixing, CI must never write.
  - `docs/internal/customer-ask.md` lines 414–415 show the exact format-check step for Phase 5 CI.
  - No `docs/internal/processes/` directory in this worktree; skip relevant-files updates until that layout exists.
---

## 2026-06-07T13:00:00Z - phase-5-core-ci-workflow-003
- Added **Go tests** step to `.github/workflows/ci.yml` running `go test ./...` from the repository root; matches `make test` in the root `Makefile` and fails the workflow on any package test failure.
- Files changed: `.github/workflows/ci.yml`
- **Learnings for future iterations:**
  - Story 003 uses the same `go test ./...` command as `make test`; no wrapper or extra flags needed.
  - `docs/internal/customer-ask.md` lines 417–418 show the exact Go test step for Phase 5 CI.
  - README validation (`make check`) lands in story 004; read-only guarantees in story 005.
---

## 2026-06-07T13:30:00Z - phase-5-core-ci-workflow-004
- Added **Custom README checks** step to `.github/workflows/ci.yml` that runs `make check` when `make` is on PATH (ubuntu-latest default) and falls back to `go run ./internal/checks` with the same pass/fail semantics; does not invoke `make links`, `make lint`, lychee, or awesome-lint.
- Files changed: `.github/workflows/ci.yml`
- **Learnings for future iterations:**
  - `make check` on ubuntu-latest is reliable because it only wraps `go run ./internal/checks`; the conditional documents the PRD fallback without changing behavior on standard runners.
  - `docs/internal/customer-ask.md` lines 420–421 show the minimal `go run ./internal/checks` step; PRD story 004 prefers `make check` first to mirror CONTRIBUTING.md local gates.
  - Story 005 covers read-only guarantees and final local regression verification across the full workflow.
---

## 2026-06-07T14:00:00Z - phase-5-workflow-integration-repair-001
- Integrated `.github/workflows/link-check.yml` from `origin/phase-5-link-awesome-maintenance-workflows`: workflow name Link Check; triggers on `pull_request` to `main` and weekly Monday schedule (`0 8 * * 1`); read-only lychee scan of README.md and docs/*.md via `lychee-action@v2` honoring root `.lychee.toml`; step comment and name identify local equivalent `make links`.
- Files changed: `.github/workflows/link-check.yml`
- **Learnings for future iterations:**
  - Source branch `phase-5-link-awesome-maintenance-workflows` has all three maintenance workflows ready to cherry-pick or copy verbatim; match `ci.yml` on main unchanged.
  - `lychee-action@v2` with `args: --verbose --no-progress README.md docs/*.md` mirrors `make links` without explicit `--config` because lychee auto-discovers `.lychee.toml` at repo root.
  - Stories 002–003 can reuse the same lychee step shape from link-check.yml inside scheduled-maintenance.yml.
---

## 2026-06-07T15:00:00Z - phase-5-workflow-integration-repair-002
- Integrated `.github/workflows/awesome-lint.yml` from `origin/phase-5-link-awesome-maintenance-workflows`: workflow name Awesome Lint; triggers on `pull_request` and `push` to `main`; read-only `npx awesome-lint` on ubuntu-latest with `actions/checkout@v4`; step comment and name identify local equivalent `npx awesome-lint`.
- Files changed: `.github/workflows/awesome-lint.yml`
- **Learnings for future iterations:**
  - awesome-lint.yml is standalone (no Node setup step needed); `npx awesome-lint` downloads and runs on ubuntu-latest without a package.json in the repo.
  - Copy maintenance workflows verbatim from `origin/phase-5-link-awesome-maintenance-workflows`; they were authored together and stay aligned with CONTRIBUTING.md story 004.
  - Story 003 scheduled-maintenance.yml combines make check + lychee steps from ci.yml and link-check.yml patterns.
---

## 2026-06-07T16:00:00Z - phase-5-workflow-integration-repair-003
- Integrated `.github/workflows/scheduled-maintenance.yml` from `origin/phase-5-link-awesome-maintenance-workflows`: workflow name Scheduled Maintenance; monthly first-of-month schedule (`0 9 1 * *`); read-only README checks via `make check` with `go run ./internal/checks` fallback; link checks via `lychee-action@v2` on README.md and docs/*.md honoring root `.lychee.toml`; step names and comments identify local equivalents `make check` and `make links`.
- Files changed: `.github/workflows/scheduled-maintenance.yml`
- **Learnings for future iterations:**
  - scheduled-maintenance.yml reuses the same make-check conditional and lychee step shape as ci.yml and link-check.yml; copy verbatim from the source branch to stay aligned.
  - Story 003 requires tests pass; `make check`, `make test`, and `go test ./...` all exit 0 after adding the workflow file (YAML-only change).
  - Story 004 updates CONTRIBUTING.md GitHub Actions subsection; story 005 verifies ci.yml unchanged on main.
---

## 2026-06-07T17:00:00Z - phase-5-workflow-integration-repair-004
- Replaced "GitHub Actions are not configured" statement in CONTRIBUTING.md with a **GitHub Actions** subsection under **Local checks**: table maps Link Check, Awesome Lint, and Scheduled Maintenance workflows to triggers and local equivalents (`make links`, `npx awesome-lint`, `make check` and `make links`); documents read-only behavior, `.lychee.toml`, and `internal/checks` as the source of README rules.
- Files changed: CONTRIBUTING.md
- **Learnings for future iterations:**
  - Copy the GitHub Actions subsection verbatim from `origin/phase-5-link-awesome-maintenance-workflows:CONTRIBUTING.md`; it was authored alongside the three maintenance workflows.
  - Story 004 is docs-only; `make check`, `make test`, `go test ./...`, and `git diff --check` all pass without code changes.
  - Story 005 verifies ci.yml unchanged; story 006 runs end-to-end convergence verification.
---

## 2026-06-07T18:00:00Z - phase-5-workflow-integration-repair-005
- Verified `.github/workflows/ci.yml` on the integration branch is byte-identical to `origin/main`: workflow name CI; `pull_request`/`push` triggers on `main`; steps use `actions/checkout@v4`, `actions/setup-go@v5` (`go-version: stable`), `go mod download`, gofmt list-only on `internal/`, `go test ./...`, and read-only README checks via `make check` with `go run ./internal/checks` fallback; no integration commit modified ci.yml.
- Files changed: none (verification-only story)
- **Learnings for future iterations:**
  - Use `git diff origin/main -- .github/workflows/ci.yml` to confirm preservation during Phase 5 integration; empty diff means stories 001–004 did not regress core CI.
  - Story 005 requires no code commit when ci.yml already matches main; mark PRD and append progress only.
  - Story 006 runs full convergence verification (all four workflows, triggers, and local quality gates).
---

## 2026-06-07T19:00:00Z - phase-5-workflow-integration-repair-006
- Verified Phase 5 workflow convergence: all four workflow files exist (ci.yml, link-check.yml, awesome-lint.yml, scheduled-maintenance.yml); trigger configs match customer ask (link check: PR + weekly `0 8 * * 1`; awesome lint: PR + push to main; scheduled maintenance: monthly `0 9 1 * *`; core CI: PR + push to main); ci.yml byte-identical to origin/main; no issue/PR templates added; integration commits limited to workflows and CONTRIBUTING.md GitHub Actions subsection.
- Ran quality gates from repository root: `make check`, `make test`, `go test ./...`, `go build ./...`, and `git diff --check` all exit 0.
- Files changed: none (verification-only story)
- **Learnings for future iterations:**
  - `origin/main` may already contain workflow content via a parallel branch (PR #9 merged phase-5-link-awesome-maintenance-workflows); use `git diff origin/main -- .github/workflows/` and blob hash comparison to confirm content convergence even when commit history diverges.
  - GitHub PR diff against main (`git diff origin/main HEAD`) differs from merge-base diff (`git diff origin/main...HEAD`); prefer direct main comparison when predicting what a PR will show.
  - Story 006 completes the PRD; create the PR when no open PR exists and all stories pass.
---

## 2026-06-07T20:00:00Z - phase-6-7-docs-convergence-cleanup-001
- Aligned README `## Contents` with ten curated categories: replaced Community navigation link with Related Lists; expanded Contributing category prose to name all ten curated sections explicitly.
- Removed Related Lists from `contentsExcludedSections` in `internal/checks/sections.go` so `make check` validates Related Lists in Contents; updated `sections_test.go` fixture accordingly.
- Files changed: README.md, internal/checks/sections.go, internal/checks/sections_test.go
- **Learnings for future iterations:**
  - `contentsExcludedSections` previously mirrored awesome-lint denylist (Related Lists omitted); Phase 6–7 convergence prioritizes taxonomy accuracy—only non-curated sections like Contributing stay excluded.
  - `## Community` remains at README footer for CODE_OF_CONDUCT/SECURITY links; it must not appear in Contents curated navigation.
  - `docs/internal/customer-ask.md` lines 85–96 show the target Contents shape with all ten category anchors.
---

## 2026-06-07T21:00:00Z - phase-6-7-docs-convergence-cleanup-002
- Modernized `docs/review-policy.md` for Phase 4 automation reality: added **Automated enforcement** section mapping Makefile targets, `internal/checks`, and four GitHub workflows to local equivalents; reframed opening to document automation plus manual maintainer review for scope, quality, category disputes, and convergence.
- Updated checklist item 8 and `needs-link-review` triage guidance to reference `make links` and Link Check workflow results instead of claiming link checking is absent.
- Renamed **Future automation (Phase 4)** to **Future automation beyond Phase 4** describing implemented checks and gaps (scope keywords, duplicate detection, banned phrases); added `resource:related-list` label for Related Lists.
- Files changed: docs/review-policy.md
- **Learnings for future iterations:**
  - Mirror CONTRIBUTING.md GitHub Actions table shape when documenting workflows in review-policy; keep workflow filenames as relative links from `docs/`.
  - Story 003 owns taxonomy.md and CONTRIBUTING cross-doc alignment; review-policy checklist section 2 already lists ten categories including Related Lists.
  - No `docs/internal/processes/` directory exists yet; skip relevant-files updates until that layout lands.
---

## 2026-06-07T22:00:00Z - phase-6-7-docs-convergence-cleanup-003
- Reconciled `docs/taxonomy.md` with ten curated categories: added `resource:related-list` label to Related Lists section and quick-reference table row; reframed Future automation section to state Phase 4 README checks and related CI are implemented and Phase 7 content seeding has not started in this batch.
- Verified `CONTRIBUTING.md` (ten categories, Local checks, GitHub Actions table, PR template pointers) and `docs/review-policy.md` checklist section 2 already aligned from stories 001–002 — no CONTRIBUTING edits required.
- Files changed: docs/taxonomy.md
- **Learnings for future iterations:**
  - Story 003 is primarily taxonomy.md quick-reference and Phase status prose; CONTRIBUTING and review-policy alignment landed in earlier Phase 4/5/6 stories.
  - Mirror review-policy `resource:related-list` label and primary-question phrasing when extending taxonomy quick-reference rows.
  - `docs/rejected.md` and `docs/historical.md` still describe Phase 4 as unimplemented; out of scope for story 003 but may need a follow-up convergence pass.
---

## 2026-06-07T23:00:00Z - phase-6-7-docs-convergence-cleanup-004
- Rewrote `factory/docs/overview.md` for Awesome AI Agent Factories: replaced AI model reference website framing with curated-list purpose; updated Read First to existing paths (`factory/docs/batch-inputs.md`, `factory/docs/batch-input-example.json`, contributor docs) and removed `docs/documentation-site-pages-needed.md`; reframed phase control around `customer-ask.md` Phases 1–10; added worktree/setup-workspace note and quality-gate commands (`make check`, `make test`, `git diff --check`).
- Files changed: factory/docs/overview.md
- **Learnings for future iterations:**
  - `factory/docs` is listed in `.gitignore` but tracked files like `overview.md` remain committable when already in git history.
  - `docs/internal/checklist.md` and `docs/internal/progress.txt` are meta-planner contract paths per ideafy AGENTS.md even when not yet created; document creation responsibility instead of listing them in Read First.
  - Story 005 is verification-only across the full reconciliation branch; create the PR only after all stories pass.
---

## 2026-06-07T23:30:00Z - phase-6-7-docs-convergence-cleanup-005
- Verified doc reconciliation quality gates on the full branch: `make check`, `make test`, `go test ./...`, and `git diff --check` all exit 0 from repository root.
- Confirmed branch diff vs `origin/main` is limited to reconciliation scope (README.md, docs/review-policy.md, docs/taxonomy.md, factory/docs/overview.md, internal/checks/sections.go, internal/checks/sections_test.go); CONTRIBUTING.md unchanged because prior Phase 4/5 stories already aligned; no Phase 7 README resource entries added.
- Files changed: none (verification-only story)
- **Learnings for future iterations:**
  - Story 005 completes the PRD; create the PR when no open PR exists and all five stories pass.
  - Use `git diff origin/main...HEAD --name-only` to confirm changed-file scope before marking verification stories complete.
  - `go build ./...` is the local typecheck equivalent when no package.json exists.
---

## 2026-06-07T16:30:00Z - phase-6-7-docs-convergence-repair-001
- Converged README `## Contents` with awesome-list conventions: Scope plus nine in-TOC curated sections (Theories through Examples and Templates); removed Related Lists from Contents navigation while retaining `## Related Lists` as the tenth curated section heading; removed awesome-toc lint disable/enable wrappers no longer needed.
- Restored `Related Lists` in `contentsExcludedSections` so `make check` enforces omission from Contents alongside Contributing; Community remains a governance footer after curated sections.
- Files changed: README.md, internal/checks/sections.go
- **Learnings for future iterations:**
  - The repair PRD corrects the prior cleanup batch: Related Lists is a curated category section but is omitted from Contents per awesome-list/`internal/checks` convention—not listed in Contents navigation.
  - awesome-toc lint disable was only needed when Related Lists appeared in Contents; removing Related Lists from TOC eliminates the awesome-lint vs checker conflict.
  - `prd.md` is authoritative when `prd.json` acceptance wording is ambiguous about Contents vs section headings.
---

## 2026-06-07T08:58:06Z - phase-6-7-docs-convergence-repair-002
- Tightened `docs/review-policy.md` for live automation with explicit manual-judgment ownership (scope fit, section-fit disputes, agent-factory relevance, quality bar, removal/relocation, convergence); documented optional `make lint`, `go test ./...`, and CONTRIBUTING **GitHub Actions** pointer; added scheduled-maintenance link scans to checklist item 8 and `needs-link-review` triage guidance.
- Files changed: docs/review-policy.md
- **Learnings for future iterations:**
  - The prior cleanup batch already landed most review-policy automation prose on main; repair story 002 is a gap-fill pass against PRD acceptance wording, not a full rewrite.
  - When checklist item 8 references link automation, cite Link Check and Scheduled Maintenance together so PR and monthly scans are both visible to reviewers.
  - Story 003 owns factory/docs/overview.md; story 004 owns taxonomy and cross-doc category alignment.
---

## 2026-06-07T09:45:00Z - phase-6-7-docs-convergence-repair-003
- Repaired `factory/docs/overview.md` for Awesome List planner guidance: tightened opening to agent-factory coordination/orchestration/flows framing; added `docs/internal/checklist.md` and `docs/internal/progress.txt` to Read First alongside `customer-ask.md`; reframed Phase Control around Awesome List build goal and planner state files; aligned Local State Files prose with Read First.
- Files changed: factory/docs/overview.md
- **Learnings for future iterations:**
  - The prior cleanup batch already removed AI model reference and `documentation-site-pages-needed.md` on main; repair story 003 is a gap-fill for PRD Read First and Phase Control wording.
  - `factory/docs` is gitignored but tracked files like `overview.md` require `git add -f` to stage edits.
  - Story 004 owns taxonomy and cross-doc category alignment; story 005 runs full convergence verification.
---

## 2026-06-07T10:30:00Z - phase-6-7-docs-convergence-repair-004
- Aligned taxonomy and cross-document category workflow language: reframed `docs/taxonomy.md` closing section to distinguish implemented Phase 4 structural checks and Phase 5 workflow gates from not-yet-started Phase 7 content seeding; expanded `factory/docs/overview.md` contributor and quality-gate prose to reference the same ten curated README categories and CONTRIBUTING local-check guidance (`make all`, optional `make lint` / `make links`, GitHub Actions table).
- Verified README section headings, CONTRIBUTING category list, review-policy checklist item 2, and taxonomy quick-reference table already name matching ten categories with no Phase 4-as-unimplemented claims outside historical/rejected sibling docs (out of scope).
- Files changed: docs/taxonomy.md, factory/docs/overview.md
- **Learnings for future iterations:**
  - Story 004 is primarily taxonomy future-automation reframing plus factory overview cross-references; README, CONTRIBUTING, and review-policy category alignment landed in prior Phase 4/5/6 stories.
  - Mirror review-policy `## Future automation beyond Phase 4` and CONTRIBUTING `#github-actions` anchors when updating taxonomy automation prose.
  - `factory/docs/overview.md` still requires `git add -f` because `factory/docs` is gitignored.
  - Story 005 is verification-only across the full repair branch; create the PR only after all stories pass.
---

## 2026-06-07T11:15:00Z - phase-6-7-docs-convergence-repair-005
- Verified documentation convergence quality gate on the full repair branch: `make check`, `make test`, `go test ./...`, `go build ./...`, and `git diff --check` all exit 0 from repository root.
- Confirmed README Contents (Scope plus nine in-TOC curated sections), CONTRIBUTING ten-category list, `docs/taxonomy.md`, `docs/review-policy.md`, and `factory/docs/overview.md` agree on implemented Phase 4/5 automation and ten curated categories; no Phase 7 README resource entries added; branch diff vs `origin/main` limited to reconciliation scope.
- Files changed: none (verification-only story)
- **Learnings for future iterations:**
  - Story 005 completes the repair PRD; create the PR when no open PR exists and all five stories pass.
  - Use `git diff origin/main...HEAD --name-only` before marking verification stories complete to confirm scope.
  - `docs/rejected.md` and `docs/historical.md` may still describe Phase 4 as unimplemented; out of scope for this repair batch per story 004 notes.
---

## 2026-06-07T11:25:00Z - phase-6-7-docs-convergence-repair-005 (mergeability)
- Restored `<!--lint disable awesome-toc-->` / `<!--lint enable awesome-toc-->` around README Contents so `npx awesome-lint` passes while `## Community` remains a governance footer omitted from Contents navigation (PR #15 awesome-lint failure).
- Files changed: README.md
- **Learnings for future iterations:**
  - awesome-toc requires every `##` heading in the TOC unless lint is disabled for the Contents block; Community exclusion from Contents still needs the disable/enable wrappers even when Related Lists is also omitted from Contents.
  - Run `npx awesome-lint` locally before creating the PR when story 005 is the convergence gate.
---

## 2026-06-07T12:00:00Z - phase-7-foundational-content-integration-repair-001
- Verified README Theories section on this branch matches `origin/phase-7-foundational-content-seed` and `origin/main` (five entries: Actor Model, An Introduction to MultiAgent Systems, Blackboard Architecture, Contract Net Protocol, Swarm Intelligence; alphabetized by link text; descriptions end with a period).
- Confirmed integration already landed via merged PR #16 (`phase-7-foundational-content-seed`); no README edit required for this story.
- Ran quality gates: `make check`, `make test`, `go build ./...`, and `git diff --check` all exit 0.
- Files changed: none (verification-only story)
- **Learnings for future iterations:**
  - Check `git diff origin/main -- README.md docs/taxonomy.md` before reapplying seed content; parallel merge (PR #16) may already converge the target files.
  - Story 005 still needs taxonomy wording update from "started in this batch" to "present on main" per repair PRD acceptance criteria.
  - Branch head equals `origin/main` until story 005 or later stories add repair-specific deltas.
---

## 2026-06-07T12:30:00Z - phase-7-foundational-content-integration-repair-002
- Verified README Coordination Patterns section on this branch matches `origin/phase-7-foundational-content-seed` and `origin/main` (four entries: Agent orchestration, AI Agent Orchestration Patterns, Building Effective Agents, Multi-agent; alphabetized by link text; descriptions end with a period).
- Confirmed integration already landed via merged PR #16 (`phase-7-foundational-content-seed`); no README edit required for this story.
- Ran quality gates: `make check`, `make test`, `go build ./...`, and `git diff --check` all exit 0.
- Files changed: none (verification-only story)
- **Learnings for future iterations:**
  - Stories 003–004 (Frameworks, Related Lists) likely follow the same verification-only pattern if PR #16 already converged all seed entries onto main.
  - Story 005 still needs taxonomy wording update from "not started" to "present on main" per repair PRD acceptance criteria.
  - No PR exists yet for this branch; create only after all six stories pass.
---

## 2026-06-07T13:00:00Z - phase-7-foundational-content-integration-repair-003
- Verified README Frameworks section on this branch matches `origin/phase-7-foundational-content-seed` and `origin/main` (five entries: AutoGen, CrewAI, LangGraph, MetaGPT, and Symphony; alphabetized by link text; descriptions emphasize orchestration/delegation/handoffs and end with a period).
- Confirmed integration already landed via merged PR #16 (`phase-7-foundational-content-seed`); no README edit required for this story.
- Ran quality gates: `make check`, `make test`, `go build ./...`, and `git diff --check` all exit 0.
- Files changed: none (verification-only story)
- **Learnings for future iterations:**
  - Story 004 (Related Lists) likely follows the same verification-only pattern if PR #16 already converged all seed entries onto main.
  - Story 005 still needs taxonomy wording update from "not started" to "present on main" per repair PRD acceptance criteria.
  - No PR exists yet for this branch; create only after all six stories pass.
---

## 2026-06-07T13:30:00Z - phase-7-foundational-content-integration-repair-004
- Verified README Related Lists section on this branch matches `origin/phase-7-foundational-content-seed` and `origin/main` (four entries: Awesome AI agents, Awesome Generative Multi-Agent Systems, Awesome Multi-Agent Learning, and Awesome Multi-Agent Papers; alphabetized by link text; descriptions end with a period).
- Confirmed integration already landed via merged PR #16 (`phase-7-foundational-content-seed`); no README edit required for this story.
- Ran quality gates: `make check`, `make test`, `go build ./...`, and `git diff --check` all exit 0.
- Files changed: none (verification-only story)
- **Learnings for future iterations:**
  - Story 005 (taxonomy Phase 7 status) is the first story likely requiring an actual edit on this branch.
  - All four seeded README sections are already on main via PR #16; remaining work is taxonomy wording and final verification.
  - No PR exists yet for this branch; create only after all six stories pass.
---

## 2026-06-07T10:10:00Z - phase-7-blog-posts-case-studies-examples-seed-001
- Seeded README Blog Posts with six alphabetized entries covering orchestration architecture (Anthropic, AWS, LangChain), distributed-systems coordination lessons, agent-orchestration engineering tradeoffs, and multi-agent failure analysis.
- Files changed: README.md
- **Learnings for future iterations:**
  - Avoid URLs already used in Coordination Patterns (e.g., anthropic.com/engineering/building-effective-agents); Blog Posts need distinct technical articles.
  - Microsoft Research blog URLs for AutoGen v0.4 returned 404 at fetch time; prefer verified stable URLs before adding entries.
  - `make check` passes with warnings when descriptions omit scope keywords; include coordination/orchestration/delegation terms naturally.
  - Stories 002–003 seed Case Studies and Examples and Templates; story 004 updates taxonomy Phase 7 status.
---

## 2026-06-07T14:00:00Z - phase-7-blog-posts-case-studies-examples-seed-002
- Seeded README Case Studies with five alphabetized deployment narratives covering insurance claims orchestration (Allianz), software maintenance swarm-coding (Faire), enterprise portfolio Copilot (Planview), financial research assistants (Schroders), and platform engineering agent hubs (Thomson Reuters).
- Files changed: README.md
- **Learnings for future iterations:**
  - `.lychee.toml` accepts HTTP 403, so Allianz media-center URLs can pass `make links` even when curl returns Forbidden without a browser user agent.
  - Case Studies differ from Blog Posts: prefer customer or vendor case narratives with named production deployments rather than architecture essays or failure analyses.
  - AWS and Google Cloud customer blogs are reliable sources for enterprise multi-agent orchestration case writeups with stable URLs.
  - Story 003 seeds Examples and Templates; story 004 updates taxonomy Phase 7 status.
---

## 2026-06-07T10:07:40Z - phase-7-blog-posts-case-studies-examples-seed-003
- Seeded README Examples and Templates with six alphabetized runnable or forkable artifacts: AutoGen Python samples, ChatDev, CrewAI Examples, GPT Researcher, LangGraph examples, and OpenAI Agents Python examples.
- Files changed: README.md
- **Learnings for future iterations:**
  - Examples and Templates should link to sample repos, example trees, or starter templates—not the main framework repos already listed under Frameworks (use `/tree/main/examples` or `-examples` repos instead).
  - `microsoft/autogen` is in Frameworks; `microsoft/autogen/tree/main/python/samples` is a distinct URL suitable for the examples section.
  - Story 004 updates taxonomy Phase 7 status; story 005 runs full batch verification.
---

## 2026-06-07T10:08:43Z - phase-7-blog-posts-case-studies-examples-seed-004
- Updated `docs/taxonomy.md` Phase 7 status paragraph to record Blog Posts, Case Studies, and Examples and Templates are seeded on main and that foundational content seeding is present across all ten curated README resource sections (Theories through Related Lists).
- Files changed: docs/taxonomy.md
- **Learnings for future iterations:**
  - Story 004 is docs-only; consolidate the Phase 7 paragraph into one sentence listing all ten section anchors rather than deferring sections to "later batch" once they are seeded.
  - Category definitions, include/exclude rules, and section headings elsewhere in taxonomy.md remain unchanged per acceptance criteria.
  - Story 005 runs full batch verification (`make check`, `make test`, `git diff --check`, section integrity); no PR exists yet until all stories pass.
---

## 2026-06-07T10:30:00Z - phase-7-blog-posts-case-studies-examples-seed-005
- Verified batch quality gates on the full branch: `make check`, `make test`, `go build ./...`, and `git diff --check` all exit 0 from repository root.
- Confirmed README diff vs `origin/main` adds entries only to Blog Posts (6), Case Studies (5), and Examples and Templates (6); Scope, Contributing, and Community sections remain present and unchanged; already-seeded sections (Theories through Related Lists) have no unintended edits.
- Confirmed changed review files are limited to README.md and docs/taxonomy.md (planning artifacts prd.json, prd.md, progress.txt stay local-only).
- Files changed: none (verification-only story)
- **Learnings for future iterations:**
  - Story 005 completes the PRD; create the PR when no open PR exists and all five stories pass.
  - Use `git diff origin/main...HEAD --name-only` to confirm changed-file scope before marking verification stories complete.
  - `go build ./...` is the local typecheck equivalent when no package.json exists.
---
